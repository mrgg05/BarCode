
@{
    ViewBag.Title = "QrOlustur";
}

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>QR Generate</title>
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.0.0/css/bootstrap.min.css" />
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.3.1/jquery.min.js"></script>
    <script src="https://maxcdn.bootstrapcdn.com/bootstrap/4.0.0/js/bootstrap.min.js"></script>
</head>
<body>
    <form id="form1" runat="server" action="QrOlustur" method="post">
        @*<div class="container">
        <h2>How to Generate QR Code in ASP.NET MVC</h2>
        <div class="row">
            <div class="col-md-9">
                <div class="form-group">
                    <label>Enter Something</label>
                    <div class="input-group">*@


        @*@foreach (var item in ViewBag.QrCode)
        {

            <p> @item.ProductName </p>
            <p> @item.ProductID </p>
            <p> @item.SKT </p>
            <p> @item.ProductionDate </p>
            <p> @item.TETT </p>

        }*@

        @*<input type="text" name="QrCode" class="form-control" value="@ViewBag.QrCode" />*@
        <div>
            @*<button type="submit">Generte QR</button>*@
            @*</div>
                        </div>
                    </div>
                </div>
            </div>*@
            @if (ViewBag.imageBytes != null)
            {
                <img src="@String.Format("data:image/png;base64,{0}", Convert.ToBase64String(ViewBag.imageBytes))" height="150" width="150" />
            }

        </div>
    </form>
</body>
</html>